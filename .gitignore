# Terraform
**/.terraform/
*.tfstate
*.tfstate.*
*.tfplan
crash.log
crash.*.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json
.terraformrc
terraform.rc

# Variáveis do Terraform (mantém só os exemplos)
*.tfvars
*.tfvars.json
!*.tfvars.example

# LocalStack (cache, binários, licença, certificados)
volume/

# Variáveis de ambiente e segredos
.env
.env.*
!.env.example

# Arquivos gerados pelo sistema operacional
.DS_Store
Thumbs.db